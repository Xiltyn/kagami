import React, {Component} from 'react';
import {MenuBar} from './menuBar';

export class App extends Component {
  render() {
    return (
      <div className="app">
        <MenuBar/>
      </div>
    );
  }
}
